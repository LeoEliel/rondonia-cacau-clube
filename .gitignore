# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log

# Node (not used, but guard against tooling)
node_modules/

# Claude Code local harness state (not shared)
.claude/

# The Flutter app keeps its own .gitignore in app/ for build artifacts.
